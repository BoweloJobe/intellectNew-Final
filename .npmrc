# Enforce the node/npm engine requirements declared in package.json.
# npm will refuse to install if the running Node or npm version is outside
# the declared engines range, preventing silently broken environments.
engine-strict=true
save-exact=true
